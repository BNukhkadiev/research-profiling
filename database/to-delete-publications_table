-- Create table for publications
CREATE TABLE publications (
    id SERIAL PRIMARY KEY,
    researcher_id INT,
    title VARCHAR(255) NOT NULL,
    date DATE,
    venue VARCHAR(255),
    core_ranking VARCHAR(10),
    topic VARCHAR(255),
    citations INT,
    FOREIGN KEY (researcher_id) REFERENCES researchers(id)
);
