-- Create table for researchers
CREATE TABLE researchers (
    id SERIAL PRIMARY KEY,
    name VARCHAR(255) NOT NULL,
    photo VARCHAR(255),
    position VARCHAR(255),
    papers INT,
    h_index INT,
    citations INT
);